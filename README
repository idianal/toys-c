toys-c
======

`toys` is a collection of (very) small programs, each meant to show just one or two competencies.


Setup
-----

## Start a toolbox container using Docker
$ sudo docker run --name toys-c -it -v "$(pwd)":/home/idianal/toys-c idianal/toolbox

## Inside the container, install required packages
$ sudo apt update
$ sudo apt install -y build-essential

## Test the environment
$ cd toys-c/foo-bar
$ git clone https://github.com/ThrowTheSwitch/Unity.git unity
$ make -s test
